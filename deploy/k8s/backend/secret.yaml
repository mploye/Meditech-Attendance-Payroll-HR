apiVersion: v1
kind: Secret
metadata:
  name: hrms-backend-secret
  namespace: hrms
  labels:
    app.kubernetes.io/part: backend
type: Opaque
stringData:
  # Replace every value below. NEVER commit real secrets.
  # Generate with: openssl rand -hex 32  /  kubectl create secret generic --from-literal
  DATABASE_URL: "postgresql+psycopg2://hrms:CHANGE_ME_DB_PASSWORD@postgres:5432/hrms"
  JWT_SECRET: "CHANGE_ME_AT_LEAST_32_CHARS"
  DEVICE_CONNECTOR_API_KEY: "CHANGE_ME_CONNECTOR_KEY"
  ESSL_USERNAME: ""
  ESSL_PASSWORD: ""
  ESSL_API_KEY: ""
  ESSL_COMPANY_SHORT_NAME: ""