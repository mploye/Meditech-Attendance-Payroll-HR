apiVersion: v1
kind: Secret
metadata:
  name: hrms-postgres-secret
  namespace: hrms
type: Opaque
stringData:
  # Must match the password used in hrms-backend-secret -> DATABASE_URL.
  POSTGRES_USER: "hrms"
  POSTGRES_PASSWORD: "CHANGE_ME_DB_PASSWORD"
  POSTGRES_DB: "hrms"